BoxLayout:
    #orientation: 'vertical'
    im_source: 'data/cityCC0.png'
    im_index: 0 
    im_color: 1.0, 1.0, 1.0, 1.0

    size_hint: None, None
    #size: 
    width: 120*app.view_size
    height: 90*app.view_size

    #canvas:
        #Color:
            #rgba: .0, .6, 0.6, 0.0
        #Rectangle:
            #pos: 0, 0
            #size: self.size

    ImageButton:
        id: image
        source: root.im_source
        #on_press: app.select_image(root.im_index)
        on_press: app.select_image(root) # 自分自身を渡す
        color: root.im_color
        nocache: True
        #color: app.get_image_color(root.im_selected)
        #size: 120*app.view_size, 90*app.view_size
    
    #CheckBox:
        #pos_hint: {'x': 0, 'y': 0}
        
